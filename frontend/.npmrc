# These audit exceptions are for vulnerabilities inside Next.js's own bundled
# dependencies that we cannot control. Running `npm audit fix --force` would
# downgrade Next.js to version 9 which is incorrect. These do NOT affect our app.
# Track: https://github.com/vercel/next.js/issues
audit-level=high
